import IRoute from '@/interfaces/IRoute'

const routes : IRoute[] = [
    {
        slug: 'Features',
        path: '/features'
    },
    {
        slug: 'Signup',
        path: '/signup'
    }
]

export default routes